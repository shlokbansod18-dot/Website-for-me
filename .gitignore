# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
/build

# local data — the SQLite database, uploaded product files and invoices
# never belong in git: they contain real customer data.
/data/

# env files hold secrets. .env.example is the safe, committed template.
.env
.env.*
!.env.example

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
.pnpm-debug.log*
/coverage
.vercel
*.tsbuildinfo
next-env.d.ts
